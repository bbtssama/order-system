<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.order.mapper.OrderItemMapper">

    <insert id="insert" parameterType="com.atguigu.order.bean.OrderItem" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO order_item (order_id, product_id, product_name, product_price, quantity, total_price)
        VALUES (#{orderId}, #{productId}, #{productName}, #{productPrice}, #{quantity}, #{totalPrice})
    </insert>

    <insert id="batchInsert">
        INSERT INTO order_item (order_id, product_id, product_name, product_price, quantity, total_price)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.orderId}, #{item.productId}, #{item.productName}, #{item.productPrice}, #{item.quantity}, #{item.totalPrice})
        </foreach>
    </insert>

    <select id="selectByOrderId" resultType="com.atguigu.order.bean.OrderItem">
        SELECT id, order_id, product_id, product_name, product_price, quantity, total_price
        FROM order_item
        WHERE order_id = #{orderId}
    </select>

</mapper>
