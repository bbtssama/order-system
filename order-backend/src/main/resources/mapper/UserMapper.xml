<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.order.mapper.UserMapper">

    <insert id="insert" parameterType="com.atguigu.order.bean.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `user` (username, password, nickname, email, phone, role)
        VALUES (#{username}, #{password}, #{nickname}, #{email}, #{phone}, #{role})
    </insert>

    <select id="selectByUsername" resultType="com.atguigu.order.bean.User">
        SELECT id, username, password, nickname, email, phone, role, create_time
        FROM `user`
        WHERE username = #{username}
    </select>

    <select id="selectById" resultType="com.atguigu.order.bean.User">
        SELECT id, username, password, nickname, email, phone, role, create_time
        FROM `user`
        WHERE id = #{id}
    </select>

</mapper>
